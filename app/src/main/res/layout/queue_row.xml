<?xml version="1.0" encoding="utf-8"?>
<!--
    A single row in the expanded queue panel: source-app icon, sender
    label, code digits in mono, and a small copy button. Tapping anywhere
    on the row copies the code; the icon-only button is a secondary
    affordance. No background fill: dividers are drawn as sibling Views
    by rebuildQueuePanel.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="44dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="6dp"
    android:paddingEnd="4dp"
    android:clickable="true"
    android:focusable="true"
    android:background="?android:attr/selectableItemBackground"
    tools:ignore="Overdraw">

    <ImageView
        android:id="@+id/q_iv_app"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:layout_marginEnd="10dp"
        android:scaleType="fitCenter"
        android:contentDescription="@null"/>

    <TextView
        android:id="@+id/q_tv_sender"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textColor="@color/text_muted"
        android:textSize="11sp"
        android:maxLines="1"
        android:ellipsize="end"/>

    <TextView
        android:id="@+id/q_tv_code"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_primary"
        android:textStyle="bold"
        android:textSize="14sp"
        android:fontFamily="@font/jetbrains_mono"
        android:letterSpacing="0.08"
        android:layout_marginEnd="6dp"/>

    <ImageView
        android:id="@+id/q_btn_copy"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_copy"
        android:padding="6dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:scaleType="fitCenter"
        android:contentDescription="@null"/>

    <!--
        Per-row delete. Drops just this code from the queue without
        copying. Icon-only, borderless ripple — same chrome as the
        copy button so the whole right side reads as a pair of
        affordances rather than a coloured button strip.
    -->
    <ImageView
        android:id="@+id/q_btn_delete"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@drawable/ic_close"
        android:padding="8dp"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:clickable="true"
        android:focusable="true"
        android:scaleType="fitCenter"
        android:contentDescription="@null"/>

</LinearLayout>
